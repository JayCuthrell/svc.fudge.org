name: Build and Deploy Landscape
on:
  push:
    branches:
      - main
permissions:
  contents: write
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Landscape
        run: |
          # 1. Prepare workspace and pre-create dist with open permissions
          mkdir -p dist
          chmod -R 777 .
          
          # 2. Run the build
          # -w /work ensures relative paths in settings.yml (like logos/svc-logo.svg) resolve correctly
          docker run --rm \
            -v $(pwd):/work \
            -w /work \
            ghcr.io/cncf/landscape2:latest \
            landscape2 build \
              --data-file data.yml \
              --settings-file settings.yml \
              --guide-file guide.yml \
              --games-file games.yml \
              --logos-path logos \
              --output-dir dist
          
          # 3. Fix ownership so the runner user can access and deploy the files
          sudo chown -R $USER:$USER dist

      - name: Add CNAME
        run: echo "svc.fudge.org" > dist/CNAME

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          branch: gh-pages